# GitLab CI/CD Pipeline pour SparkFit Backend
# Se déclenche uniquement sur les branches develop et main

stages:
  - test
  - build

variables:
  # Variables globales
  NODE_VERSION: "18"
  POSTGRES_VERSION: "14"
  
  # Variables d'environnement pour les tests
  NODE_ENV: "test"
  JWT_SECRET: "test-secret-key"
  PORT: "3001"
  DATABASE_URL: "postgresql://testuser:testpassword@postgres-test:5432/testdb"

# Cache pour les dépendances Node.js
cache:
  key: 
    files:
      - sparkfit_backend/package-lock.json
  paths:
    - sparkfit_backend/node_modules/
    - .npm/

# Service PostgreSQL pour les tests
services:
  - name: postgres:14
    alias: postgres-test
    variables:
      POSTGRES_DB: testdb
      POSTGRES_USER: testuser
      POSTGRES_PASSWORD: testpassword

# Job de test
test:
  stage: test
  image: node:${NODE_VERSION}-alpine
  only:
    - develop
    - main
  except:
    - tags
  before_script:
    # Installer les dépendances système nécessaires
    - apk add --no-cache git
    
    # Aller dans le répertoire backend
    - cd sparkfit_backend
    
    # Installer les dépendances
    - npm ci
    
    # Installer Prisma CLI
    - npm install -g prisma
    
    # Copier le schéma Prisma depuis le répertoire parent
    - cp ../sparkfit_prisma-schema/schema.prisma ./prisma/schema.prisma
    - cp -r ../sparkfit_prisma-schema/migrations ./prisma/
    
    # Générer le client Prisma
    - npx prisma generate --schema=./prisma/schema.prisma
    
    # Attendre que PostgreSQL soit prêt
    - echo "Attente de PostgreSQL..."
    - while ! nc -z postgres-test 5432; do sleep 1; done
    
    # Appliquer les migrations de test
    - npx prisma migrate deploy --schema=./prisma/schema.prisma
    
    # Vérifier la connexion à la base de données
    - npx prisma db push --schema=./prisma/schema.prisma --accept-data-loss
  script:
    # Exécuter les tests avec couverture
    - npm run test:coverage
    
    # Vérifier que les tests passent
    - echo "✅ Tous les tests sont passés avec succès"
  after_script:
    # Nettoyer les données de test
    - npx prisma migrate reset --schema=./prisma/schema.prisma --force
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: sparkfit_backend/coverage/cobertura-coverage.xml
    paths:
      - sparkfit_backend/coverage/
    expire_in: 1 week
    when: always
  coverage: '/All files[^|]*\|[^|]*\s+([\d\.]+)/'
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "develop"
      when: always
    - if: $CI_COMMIT_BRANCH == "main"
      when: always

# Job de build
build:
  stage: build
  image: docker:20.10.16
  services:
    - docker:20.10.16-dind
  only:
    - develop
    - main
  except:
    - tags
  before_script:
    # Se connecter au daemon Docker
    - docker info
  script:
    # Construire l'image Docker
    - echo "🔨 Construction de l'image Docker..."
    - docker build -t sparkfit-backend:$CI_COMMIT_SHA -t sparkfit-backend:latest -f sparkfit_backend/Dockerfile .
    
    # Vérifier que l'image a été construite
    - docker images | grep sparkfit-backend
    
    # Test de l'image (vérification de base)
    - echo "🧪 Test de l'image Docker..."
    - docker run --rm --name test-container sparkfit-backend:$CI_COMMIT_SHA node --version
    
    # Nettoyer les images temporaires
    - docker image prune -f
  after_script:
    # Nettoyer les images de test
    - docker rmi sparkfit-backend:$CI_COMMIT_SHA || true
    - docker rmi sparkfit-backend:latest || true
  artifacts:
    paths:
      - docker-images/
    expire_in: 1 week
    when: on_success
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "develop"
      when: always
    - if: $CI_COMMIT_BRANCH == "main"
      when: always

# Job de validation du code (optionnel)
lint:
  stage: test
  image: node:${NODE_VERSION}-alpine
  only:
    - develop
    - main
  except:
    - tags
  before_script:
    - cd sparkfit_backend
    - npm ci
  script:
    # Vérifier la syntaxe JavaScript
    - echo "🔍 Validation de la syntaxe JavaScript..."
    - node -c src/app.js
    - node -c src/controllers/*.js
    - node -c src/routes/*.js
    - node -c src/middlewares/*.js
    
    # Vérifier les imports
    - echo "📦 Validation des imports..."
    - node -e "require('./src/app.js'); console.log('✅ Tous les imports sont valides')"
    
    # Vérifier la configuration Prisma
    - echo "🗄️ Validation du schéma Prisma..."
    - npx prisma validate --schema=../sparkfit_prisma-schema/schema.prisma
    
    echo "✅ Validation du code terminée"
  allow_failure: true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      when: never
    - if: $CI_COMMIT_BRANCH == "develop"
      when: always
    - if: $CI_COMMIT_BRANCH == "main"
      when: always 